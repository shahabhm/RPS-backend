export const sendSMS = function (phoneNumber: string, text: string) : Promise<void> {
    // TODO: replace with real code
    console.log(`Sending SMS to ${phoneNumber}: ${text}`);
    return Promise.resolve();
}